import React from 'react';

const Header = () => {
    return (
        <header className="app-header">
            <div className="container">
                <h1 className="header-title">Infinite Scrolling</h1>
            </div>
        </header>
    );
};

export default Header;
